package fi.foyt.fni.persistence.model.oauth;

public enum OAuthClientType {

  /* Client is used by an service (two-legged) */
  
  SERVICE,

  /* Client is used by users (three-legged) */
  
  USER
  
}
