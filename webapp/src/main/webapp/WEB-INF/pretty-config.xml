<pretty-config xmlns="http://ocpsoft.org/schema/rewrite-config-prettyfaces" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://ocpsoft.org/schema/rewrite-config-prettyfaces http://ocpsoft.org/xml/ns/prettyfaces/rewrite-config-prettyfaces.xsd">

  <rewrite match="^/forge$" substitute="/forge/" />
  <rewrite match="^/forge([\?\#])(.*)" substitute="/forge/$1$2" />

  <rewrite match="^/gamelibrary$" substitute="/gamelibrary/" />
  <rewrite match="^/gamelibrary([\?\#])(.*)" substitute="/gamelibrary/$1$2" />

  <rewrite match="^/login$" substitute="/login/" />
  <rewrite match="^/login([\?\#])(.*)" substitute="/login/$1$2" />

</pretty-config>